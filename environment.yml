name: mortie
channels:
  - conda-forge
  - defaults
dependencies:
  - flake8
  # Tracks the package floor in pyproject.toml (issue #187): mortie's word
  # scalars need NEP 50.
  - numpy>=2
  - pytest
  - pytest-cov
  - pip
  - pip:
    - healpy
